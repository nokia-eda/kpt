apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: eda-api-ingress-https
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: Sample ingress config that connects to the eda-api service using HTTPS
